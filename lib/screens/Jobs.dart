import 'package:flutter/material.dart';
import 'package:get/get.dart';

class jobpage extends StatelessWidget {
  const jobpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('this is  jobs page'),
    );
  }
}
