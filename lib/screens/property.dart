import 'package:flutter/material.dart';
import 'package:get/get.dart';

class propertypage extends StatelessWidget {
  const propertypage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('this is property page'),
    );
  }
}
