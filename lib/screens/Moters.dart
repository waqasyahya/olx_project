import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Moterpage extends StatelessWidget {
  const Moterpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('this is Moters page'),
    );
  }
}
