import 'package:flutter/material.dart';
import 'package:get/get.dart';

class Carspage extends StatelessWidget {
  const Carspage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('this is car page'),
    );
  }
}
